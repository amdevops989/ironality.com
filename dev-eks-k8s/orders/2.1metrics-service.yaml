apiVersion: v1
kind: Service
metadata:
  name: orders-metrics
  namespace: demo
  labels:
    app: orders
    metrics: "true"
spec:
  selector:
    app: orders
  ports:
    - name: metrics
      port: 9467
      targetPort: 9467
      protocol: TCP

