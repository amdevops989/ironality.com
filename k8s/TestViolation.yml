apiVersion: v1
kind: Pod
metadata:
  name: test-violation
  namespace: demo
spec:
  containers:
    - name: nginx
      image: nginx:latest   # ❌ violates no-latest-tag
      securityContext:
        runAsUser: 0        # ❌ violates require-non-root

